export type Session = {
  user?: {
    id: number;
    email: string;
    username: string;
  };
};

export type Locals = {
  user?: {
    id: number;
    email: string;
    username: string;
  };
};
